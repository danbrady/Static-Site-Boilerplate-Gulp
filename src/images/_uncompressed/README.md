Placing any PNG, GIF, JPG, or SVG files into this folder will trigger a gulp task to create a compressed copy in '[build]images/'.

Example:
* `src/images/_uncompressed/image.png` --> `build/images/image.png`

